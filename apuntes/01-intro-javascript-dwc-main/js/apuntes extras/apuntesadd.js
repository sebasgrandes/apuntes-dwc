// recuerda que una propiedad de un objeti es el nombre y el valor, ambos, no solo 1

// ! recuerda que los parametros (o parametros formales) son los que van en la funcion. mientras que los argumentos (o parametros reales), son los valores que reemplazan a los parametros al ser llamados
function producto(par1, par2) {
    console.log(`la primera es ${par1}, la segunda es ${par2}`);
}
producto(arg1, arg2);
