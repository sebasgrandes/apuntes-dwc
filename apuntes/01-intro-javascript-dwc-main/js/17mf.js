const numero1 = 20;
const numero2 = "79";

console.log(parseInt(numero2)); // parseInt() es una FUNCIÓN (convierte string a numero)
console.log(numero1.toString()); // .toString() es un MÉTODO (convierte numero en string)
